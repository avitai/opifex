apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: frontend-service-monitor
  namespace: opifex-community
  labels:
    app.kubernetes.io/name: frontend-service-monitor
    app.kubernetes.io/component: monitoring
    app.kubernetes.io/part-of: opifex-community
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: frontend-service
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
      honorLabels: true
