apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: workspace-service-monitor
  namespace: opifex-community
  labels:
    app.kubernetes.io/name: workspace-service-monitor
    app.kubernetes.io/component: monitoring
    app.kubernetes.io/part-of: opifex-community
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: workspace-service
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
      honorLabels: true
