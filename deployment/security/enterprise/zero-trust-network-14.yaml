apiVersion: v1
kind: Service
metadata:
  name: risk-scoring-service
  namespace: security
  labels:
    app.kubernetes.io/name: risk-scoring-service
    app.kubernetes.io/component: zero-trust
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: risk-scoring-service
