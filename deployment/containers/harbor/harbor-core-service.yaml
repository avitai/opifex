# Harbor Core Service
apiVersion: v1
kind: Service
metadata:
  name: harbor-core
  namespace: harbor-system
  labels:
    app: harbor-core
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: harbor-core
