# ShellCheck configuration file
# Disable style warnings for reasonable patterns

# SC2181: Check exit code directly with e.g. 'if mycmd;', not indirectly with $?
# This is often more readable when the command output is captured separately
disable=SC2181
